[project]
name = "python-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "python projects", email = "harunmd1143@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.4.2",
    "langchain-openai>=1.6.5",
    "langgraph>=1.2.12",
    "python-dotenv>=1.2.3",
]

[project.scripts]
python-project = "python_project:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
